// Team Reducer

const teams = (state=[], action) => {

  switch(action.type){
    case 'ADD':
      return {
        
      };
    case 'D':
      return {

      };
    default :
      return state;
  }
}// end teams

export default teams;
